import Vue from 'vue';
import vuex from 'vuex';
// import { stat } from 'fs';
Vue.use(vuex);

export default new vuex.Store({
    state: {},
    getters: {
        // isUserId(state) {
        //     return state.userId.length > 0;
        // }
    },
    mutations: {},
    actions: {

    }
});